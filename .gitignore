node_modules/
build/
dist/
coverage/
.DS_Store
*.log
*.db
*.db-journal
.env*
!.env.example
!config/template.env
!config/.env.example
.turbo/
.agent_private_key.pem
kudbee_telemetry_db.json

# IDE / editor tooling
.cursor-server
.idea/
.vscode-server
.windsurf-server
.lightning_studio/

# Claude (credentials, IDE locks, backups)
.claude/
.claude.json

# Local Ollama (keys, history)
.ollama/

# ── MEMORY SEPARATION (THINK Protocol architectural rule) ──────────────
# Operational memory (ephemeral) — Redis/runtime state, regenerated every
# session, never mergeable source. Lives outside git.
.kilo/memory/bus/
.kilo/memory/dthink/
.kilo/memory/forge/
.kilo/memory/gate-results.json
.kilo/memory/journal.json
# THINKBOX runtime workspaces — derived detection state, never mergeable source.
.kilo/thinkbox/

# Durable engineering knowledge (learned) — committed intentionally.
# .kilo/memory/learnings/, .kilo/memory/snippets/, .kilo/memory/decisions/
# are the institutional knowledge of the Engineering OS and ARE versioned.
