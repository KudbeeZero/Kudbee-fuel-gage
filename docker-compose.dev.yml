version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "5173:5173"
    env_file:
      - ../.env
    volumes:
      - .:/workspace
      - /workspace/node_modules
    stdin_open: true
    tty: true
