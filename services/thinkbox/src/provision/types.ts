export type ProvisionPhase = 'pending' | 'runtime_detection' | 'dependency_install' | 'service_setup' | 'env_config' | 'build' | 'test' | 'deploy' | 'ready';
export type ProvisionStatus = 'queued' | 'running' | 'complete' | 'failed' | 'skipped';
export interface ProvisionStep { id: string; phase: ProvisionPhase; label: string; command: string | null; status: ProvisionStatus; reason: string; dependsOn: string[]; evidence: string[]; risk: string; estimatedDurationMs: number | null; }
export interface WorkspaceProvisionPlan { workspaceId: string; plannedAt: string; summary: string; simulation: boolean; totalSteps: number; estimatedTotalDurationMs: number; warnings: string[]; risks: Array<{ severity: string; message: string }>; phases: Record<string, ProvisionStep[]>; orderedSteps: string[]; graph: { nodes: any[]; edges: any[]; rootId: string }; timeline: any[]; readyScore: { total: number; grade: string; runtime: number; dependencies: number; environment: number; ci: number; deploy: number; risk: number; documentation?: number; recommendedNextAction: string; breakdown: Record<string, { score: number; maxScore: number; issues: string[] }> }; }
